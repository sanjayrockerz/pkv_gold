'use client';
import { useState } from 'react';
const principles = [['TRANSPARENT PROCESS', 'The sequence stays visible: measure, assess, value.'], ['CLEAR COMMUNICATION', 'The inputs and indicative result are explained in plain language.'], ['VISIBLE VALUATION', 'The calculation is shown before the final in-person assessment.'], ['REAL CUSTOMER EXPERIENCE', 'Customer stories will be published here once approved source material is available.']] as const;
export function Trust() { const [selected, setSelected] = useState(0); return <section className="trust-scene scene scene-dark" aria-labelledby="trust-heading"><div className="trust-copy"><span className="scene-index">07 / THE DIFFERENCE</span><h2 id="trust-heading">A RATE<br /><em>ISN&apos;T THE WHOLE STORY.</em></h2><p>Market rate is one input. Weight, purity, and applicable terms shape the estimated value that follows.</p><div className="trust-principles">{principles.map(([title, detail], index) => <button type="button" className={selected === index ? 'is-selected' : ''} aria-expanded={selected === index} key={title} onClick={() => setSelected(index)}><span>0{index + 1}</span><strong>{title}</strong>{selected === index && <small>{detail}</small>}</button>)}</div></div><div className="trust-diagram" aria-hidden="true"><span>RATE / GRAM</span><b>↓</b><span>WEIGHT</span><b>↓</b><span>PURITY</span><b>↓</b><strong>ESTIMATED<br />VALUE</strong><small>FINAL VALUATION IS CONFIRMED AFTER PHYSICAL ASSESSMENT.</small></div></section>; }
