'use client';
import { useState } from 'react';
import { purityOptions } from '@/lib/constants';
export function Purity() { const [selectedKarat, setSelectedKarat] = useState(22); const selected = purityOptions.find((option) => option.karat === selectedKarat) ?? purityOptions[1]; return <section className="purity-scene scene scene-deep" aria-labelledby="purity-heading"><div className="purity-intro"><span className="scene-index">04 / PURITY</span><h2 id="purity-heading">PURITY<br /><em>CHANGES VALUE.</em></h2><p>Every karat tells a different story of gold content. Select a level to see the relationship clearly.</p></div><div className="purity-field"><div className="purity-field-head"><span>GOLD CONTENT</span><strong>{selected.karat}K <i>{selected.purity}%</i></strong></div><div className="purity-choices" role="tablist" aria-label="Select purity"><div className="purity-beam" aria-hidden="true" />{purityOptions.map((option) => <button id={`purity-tab-${option.karat}`} type="button" role="tab" aria-controls="purity-panel" aria-selected={selectedKarat === option.karat} tabIndex={selectedKarat === option.karat ? 0 : -1} className={selectedKarat === option.karat ? 'selected' : ''} key={option.karat} onClick={() => setSelectedKarat(option.karat)}><strong>{option.karat}K</strong><span>{option.purity}%</span></button>)}</div><div id="purity-panel" className="purity-detail" role="tabpanel" aria-live="polite"><span>PURITY FACTOR</span><strong>{selected.factor.toFixed(3)} <i>/ gold content</i></strong><p>Rate comes from the PKV Gold admin-controlled daily value.</p></div></div></section>; }
