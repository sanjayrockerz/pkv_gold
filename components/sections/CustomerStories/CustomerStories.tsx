'use client';
import { useState } from 'react';
const stories = [{ quote: 'Approved customer story will appear here.', name: 'CUSTOMER STORY / PENDING APPROVAL', place: 'Kolathur' }];
export function CustomerStories() { const [index, setIndex] = useState(0); const story = stories[index]; return <section className="stories-scene scene scene-dark" aria-labelledby="stories-heading"><div className="stories-meta"><span className="scene-index">09 / CUSTOMER STORIES</span><h2 id="stories-heading">REAL EXPERIENCE.<br /><em>CAREFULLY TOLD.</em></h2><span className="story-count">0{index + 1} / 0{stories.length}</span></div><div className="story-content"><blockquote>“{story.quote}”</blockquote><footer><strong>{story.name}</strong><span>{story.place}</span></footer><div className="story-controls"><button type="button" aria-label="Previous story" disabled={index === 0} onClick={() => setIndex(Math.max(0, index - 1))}>←</button><button type="button" aria-label="Next story" disabled={index === stories.length - 1} onClick={() => setIndex(Math.min(stories.length - 1, index + 1))}>→</button></div></div></section>; }
