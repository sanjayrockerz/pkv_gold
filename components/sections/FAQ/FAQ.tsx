'use client';
import { useState } from 'react';
const questions = [['How is gold value calculated?', 'The calculator uses weight and the selected purity rate to produce an indicative value.'], ['Which purity levels can be assessed?', 'The configured calculator currently includes 24K, 22K, 18K, and 14K options.'], ['What should I bring?', 'This answer is pending confirmation of PKV’s approved customer guidance.'], ['How does the valuation process work?', 'The experience is structured around bringing the piece, measuring, assessing, valuing, and discussing settlement.'], ['How is the final valuation determined?', 'The final valuation is confirmed after physical assessment; the calculator result is not a guaranteed payout.']] as const;
export function FAQ() { const [open, setOpen] = useState<number | null>(0); return <section className="faq-scene scene scene-dark" aria-labelledby="faq-heading"><div className="faq-intro"><span className="scene-index">12 / QUESTIONS</span><h2 id="faq-heading">A FEW THINGS<br /><em>WORTH KNOWING.</em></h2></div><div className="faq-list">{questions.map(([question, answer], index) => { const answerId = `faq-answer-${index}`; const isOpen = open === index; return <div className={`faq-item ${isOpen ? 'is-open' : ''}`} key={question}><button id={`faq-question-${index}`} type="button" aria-expanded={isOpen} aria-controls={answerId} onClick={() => setOpen(isOpen ? null : index)}><span>{question}</span><b aria-hidden="true">{isOpen ? '−' : '+'}</b></button><div id={answerId} className="faq-answer" role="region" aria-labelledby={`faq-question-${index}`} hidden={!isOpen}><p>{answer}</p></div></div>; })}</div></section>; }
